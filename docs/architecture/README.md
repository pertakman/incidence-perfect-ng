# Architecture Notes

- `ui-framework-diagram.txt`: legacy architecture sketch from early UI design.

Treat this as historical context. Check current source in `src/` for the authoritative implementation.